# Weekly improve report — 2026-09-07 11:26

Claude proposed **5** change(s).

## ✅ Applied (in this PR)
- **Investigate repeated bookings_found=1 with sms_sent=0** (`agent.py`, impact: low) — From 2026-08-25 through 2026-08-26 the booking_agent logged bookings_found=1 with sms_sent=0 on ~23 consecutive runs. This is the expected behaviour when a booking is already in seen_ids (deduplicated), BUT there is no counter distinguishing 'already seen' from 'SMS failed'. In agent.py, when a booking is already seen and is a Förfrågan/Offert it logs a PENDING warning every single run forever, and the run summary shows bookings_found=1/sms_sent=0 with no indication whether it's a stuck pending booking. Add an 'already_seen' and 'pending' counter to the run summary so improve.py and the owner can distinguish a healthy dedupe from a stuck booking that never gets Hanterad.
- **Include already_seen and pending counters in run summary** (`agent.py`, impact: low) — The run_history.jsonl only records bookings_found/sms_sent/sms_failed. Given the long stretch of bookings_found=1,sms_sent=0 entries, improve.py cannot tell whether these are healthy dedupes or a stuck Förfrågan. Emit the new counters (added in the companion change) so the pattern is visible in the structured history.
- **Guard against missing email field in build_sms causing empty Email line** (`agent.py`, impact: low) — In build_sms, driver_email is included whenever truthy, but there is no validation that it is not a stray mailto value like 'mailto:' with empty tail. Minor robustness — strip whitespace and ignore emails without '@'.

## ⚠️ Proposed but NOT applied (review manually)
- **SKILL.md describes email-based quote flow but parts_agent.py and email_agent.py are missing** (`agent.py`, config_change) — parts_agent.py and email_agent.py are reported as 'FILE NOT FOUND'. agent.py imports email_trigger.should_run but the Förfrågan flow described in SKILL.md (launching parts_agent.py) is not present in agent.py at all. This is a real gap: SKILL.md and the header of agent.py claim Förfrågan bookings launch parts_agent.py, but the code only logs a PENDING warning and never calls it. Either the files need to exist or the docs/code need reconciling. Flagging for manual attention — cannot auto-generate the missing agents safely.
- **mark_booking_handled opens a full second browser/login per booking** (`portal_scraper.py`, config_change) — mark_booking_handled launches a brand-new Playwright browser and re-logs into the portal for every single confirmed Bokning, after fetch_bookings already had an authenticated session. On days with 2-3 bookings this triples login time and login-failure risk. This is a structural performance/reliability concern noted in SKILL.md priority 5 (reduce browser open time). Recommend performing the Hanterad click within the existing fetch_bookings session or reusing a persistent context. Flagged for manual review.
