# Weekly improve report — 2026-06-29 10:49

Claude proposed **4** change(s).

## ✅ Applied (in this PR)
- **Förfrågan/Offert bookings re-notified every run causing repeated PENDING warnings and no SMS** (`agent.py`, impact: low) — Run history shows bookings_found=1..4 with sms_sent=0 across dozens of consecutive runs (e.g. 2026-06-17 14:07 onward, and 2026-06-24 all 4 found 0 sent). This matches the agent.py logic: once a Förfrågan/Offert is in seen_ids it is skipped (only a PENDING warning logged), but it keeps being counted in bookings_found because it stays on the Nya tab forever (no Hanterad button exists). The repeated bookings_found>0 / sms_sent=0 pattern is the visible symptom — owner gets nothing actionable and history looks like a stuck queue. The first time a Förfrågan is seen the code DOES send SMS (new_count++) but then calls mark_booking_handled which returns None; subsequent runs skip it. This is functionally correct for SMS but the run summary is misleading and there is no signal distinguishing 'nothing new' from 'stuck pending items'.
- **parts_agent.py and email_agent.py are missing but referenced by SKILL.md and workflow** (`agent.py`, impact: high) — SKILL.md and the system description state agent.py launches parts_agent.py for Förfrågan and email_agent.py runs every 30 min, but both files are reported as 'FILE NOT FOUND'. agent.py itself does NOT import or call parts_agent at all — the Förfrågan branch only logs 'Handle manually'. This is a documentation/behavior mismatch: the advertised parts-quote automation does not exist in the running code. Cannot safely auto-generate these large files; flagging for manual creation/restoration.
- **Login submit click may not wait for navigation, causing flaky post-login state** (`portal_scraper.py`, impact: medium) — In _fill_login, the submit selector is clicked with page.click(sel) and returns immediately; fetch_bookings then relies on wait_for_url with a lambda that compares url != PORTAL_LOGIN_URL. PORTAL_LOGIN_URL is 'https://meca.promeisterportal.com/' but after login the browser may redirect through the same base path briefly, and the wait_for_url is wrapped in a try/except PWTimeout that silently passes. If login silently fails, the code proceeds to scrape and logs 'No booking links found' rather than a clear login failure. The mark_booking_handled login path uses an even weaker lambda (url != PORTAL_LOGIN_URL only). Add an explicit post-login verification that we are NOT still on the login form before continuing.
- **qty index uses cells[3] without verifying SEK column position** (`portal_scraper.py`, impact: medium) — In _extract_specification, qty = cells[3].inner_text() assumes the 4th cell is quantity, but the same function searches for the price by scanning reversed cells for 'SEK', implying column layout is not fixed. If the table has fewer/extra columns, qty will be wrong (e.g. picking up a price or row index), which then feeds build_sms as 'x4' etc. This is the kind of silent data-quality bug SKILL.md flags under 'SMS content quality'. Guard the qty extraction to only accept short numeric values.
