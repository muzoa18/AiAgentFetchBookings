# Weekly improve report — 2026-09-14 11:36

Claude proposed **5** change(s).

## ✅ Applied (in this PR)
- **Detect repeated bookings_found>0 with sms_sent=0 (silent re-processing)** (`agent.py`, impact: low) — Run history shows many runs like 2026-09-12 where bookings_found=1 but sms_sent=0 across 4 consecutive runs. This is expected when a booking is already in seen_ids, but the log line uses log.info at INFO level 'Already handled' which makes it hard to distinguish a genuine 'nothing new' run from a run that found bookings but sent no SMS. The run summary should record how many were skipped as already-seen so improve.py can tell the difference between healthy dedup and a broken SMS path.
- **Record skipped-as-seen count in run summary** (`agent.py`, impact: low) — Because bookings_found counts ALL links on the Nya tab (not just new ones), a run showing bookings_found=1, sms_sent=0 is ambiguous. Adding skipped_seen to the run_logger summary lets improve.py and monitoring distinguish 'all already handled' from 'SMS silently failing'. Pairs with the counter added in proposal 1.
- **Increment skipped_seen when a booking is already handled** (`agent.py`, impact: low) — The dedup branch 'if booking_id in seen_ids' currently just continues without counting. To make the run summary in proposal 2 accurate, increment skipped_seen there. This clarifies the many bookings_found>0/sms_sent=0 runs in the history.
- **Hanterad clicked for ALL booking types, not only Bokning** (`agent.py`, impact: medium) — SKILL.md and agent docstring state Hanterad should only be clicked for Bokning; Förfrágan/Offert have no button. But agent.py calls mark_booking_handled(config, detail_url) unconditionally for every booking that got an SMS, regardless of booking_type. mark_booking_handled does return None gracefully when no button is found, but it always opens a second full browser+login session for every Förfrågan/Offert, wasting a login and time. Guard the call on booking_type == 'Bokning'.
- **Login success not verified — may proceed on failed login** (`portal_scraper.py`, impact: medium) — In fetch_bookings the wait_for_url after _fill_login swallows PWTimeout with 'pass' and continues. If login actually failed (wrong credentials, extra field), the code proceeds to look for the booking table and simply logs 'No booking links found', masking an auth failure as an empty Nya tab. Add an explicit check that we are no longer on the login page (login form still present => fail fast with screenshot).
